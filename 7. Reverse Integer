class Solution:
    def reverse(self, x: int) -> int:
        if x == 0:
            return 0

        MIN = -2147483648
        MAX = 2147483648

        ans = 0
        while x != 0:
            digit = int(math.fmod(x, 10)) #this was done due to the problems with the negative integer handled differently in python
            x = int(x/10)

            if (ans > MAX // 10 or (ans == MAX // 10 and digit >= MAX % 10)):
                return 0
            if (ans < MIN // 10 or (ans == MIN // 10 and digit <= MIN % 10)):
                return 0
            ans = (ans * 10) + digit
        return ans
